# ============================================
# ClawDeckX Web 环境变量配置
# 复制此文件为 .env 并修改对应值
# 加载优先级：命令行参数 > 环境变量 > 配置文件 > 默认值
# ============================================

# ---- 服务器 ----
CLAWMGR_PORT=3847                    # Web 服务端口
CLAWMGR_BIND=127.0.0.1              # 绑定地址（默认仅本地访问，改为 0.0.0.0 需注意安全）

# ---- 数据库 ----
CLAWMGR_DB_DRIVER=sqlite             # 数据库驱动：sqlite | postgres
CLAWMGR_DB_SQLITE_PATH=              # SQLite 文件路径（默认 ~/.openclaw/clawmgr.db）
CLAWMGR_DB_DSN=                      # PostgreSQL 连接串（仅 driver=postgres 时生效）
                                     # 示例：host=db.xxx.supabase.co user=postgres password=xxx dbname=clawmgr port=5432 sslmode=require

# ---- 鉴权 ----
CLAWMGR_JWT_SECRET=                  # JWT 签名密钥（留空则首次启动自动生成）
CLAWMGR_JWT_EXPIRE=24h              # JWT 过期时间

# ---- 日志 ----
CLAWMGR_LOG_LEVEL=info               # 日志级别：trace | debug | info | warn | error | fatal
CLAWMGR_LOG_MODE=production          # 日志模式：debug（彩色控制台）| production（JSON 文件）
CLAWMGR_LOG_FILE=                    # 日志文件路径（默认 ~/.openclaw/clawmgr.log）

# ---- OpenClaw ----
CLAWMGR_OPENCLAW_CONFIG_PATH=        # OpenClaw 配置目录（默认 ~/.openclaw）
CLAWMGR_OPENCLAW_GATEWAY_PORT=18789  # OpenClaw Gateway 端口

# ---- 监控 ----
CLAWMGR_MONITOR_INTERVAL=30          # 监控检查间隔（秒）
CLAWMGR_MONITOR_AUTO_RESTART=true    # 网关异常自动重启
CLAWMGR_MONITOR_MAX_RESTART=3        # 最大连续重启次数

# ---- 告警 ----
CLAWMGR_ALERT_ENABLED=false          # 告警总开关
CLAWMGR_ALERT_WEBHOOK_URL=           # Webhook 告警地址
